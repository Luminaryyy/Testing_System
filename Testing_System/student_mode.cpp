#include "student_mode.h"

void check_student(const string& fileName, string *login, string *password)
{
	ifstream file(fileName);

	string line;

}

void MODE_MAIN()
{
	string login, password;
	ifstream file();
}